{% extends './_base.twig' %}

{% block main %}
  <h1>
    {{ siteTitle }}
  </h1>
  <h2
    data-module="Hello"
    data-context="@media (min-width: 1024px) and was @visible"
  >
    {{ data[lang].title }}
  </h2>

{% endblock %}